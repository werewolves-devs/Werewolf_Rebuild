def signup(user_id,emoji) -> bool:
    # This function signs up a new user for a game.
    # One must make sure the Game Master have opened the signups.
    # Users are signed up as spectators by default.
    
    return False # TODO