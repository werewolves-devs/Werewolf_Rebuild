def clear_database(file_name='storage/game.db') -> None:
    # This function should drop all tables of a database.
    # One may assume this is the database of a regular game, not the general database.
    return

def build_database(file_name='storage/game.db') -> None:
    # This function needs to build a given file into a functional database with the correct tables.
    # The existing database may be an empty file or a recently cleared database.
    return